<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:noesis="clr-namespace:NoesisGUIExtensions"
    mc:Ignorable="d"
    xmlns:local="clr-namespace:AM"
    d:DesignWidth="1280" d:DesignHeight="720"
    x:Name="WindowManager">
    <UserControl.Resources>

        <!-- All window templates -->
        <DataTemplate x:Key="SackContainerTemplate">
            <local:SackContainerPanel Width="1200"/>
        </DataTemplate>

        <local:NoesisWindowManager x:Key="WindowDataModel"/>
        <local:WindowDataTemplateSelector x:Key="ViewModelTemplateSelector"
            SackContainerTemplate="{StaticResource SackContainerTemplate}"/>

    </UserControl.Resources>
    <Grid Background="Transparent" DataContext="{StaticResource WindowDataModel}">
        <ItemsControl ItemsSource="{Binding ActiveWindows}" ItemTemplateSelector="{StaticResource ViewModelTemplateSelector}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    <Setter Property="MaxWidth" Value="{Binding MaxWidth}"/>
                    <Setter Property="MaxHeight" Value="{Binding MaxHeight}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>
    </Grid>
</UserControl>